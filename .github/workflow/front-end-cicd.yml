name: Upload Website

on:
  workflow_dispatch:
  push:
    branches:
    - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - uses: jakejarvis/s3-sync-action@master
      with:
        args: --acl private --follow-symlinks --delete
      env:
        AWS_ACCESS_KEY_ID: ${{ AKIA47CRVWPHWBPOELWV }}
        AWS_SECRET_ACCESS_KEY: ${{ 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 }}
        AWS_REGION: 'eu-central-1'
        SOURCE_DIR: 'Resume'